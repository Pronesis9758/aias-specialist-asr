project:
  name: AI Specialist 제조 음성 ASR
  owner: Pronesis9758
  seed: 42

paths:
  manifest: data/private/manifest.csv
  domain_terms: data/domain_terms/manufacturing_terms.csv
  artifacts_dir: artifacts/runs
  database: backdata/experiments.sqlite3
  model_lock: models/model-lock.yaml

model:
  backend: faster_whisper
  repo_id: Systran/faster-whisper-tiny
  revision: main
  local_dir: models/faster-whisper-tiny
  language: ko
  device: auto
  compute_type: auto
  beam_size: 5

correction:
  enabled: true
  case_sensitive: false

training:
  enabled: false
  reason: Baseline과 데이터 품질을 먼저 확정

report:
  title: 제조 음성 ASR Baseline 평가 보고서
  include_charts: true
