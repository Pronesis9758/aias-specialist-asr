benchmark:
  id: fixture-whisper-model-benchmark-v1
  name: Whisper 모델 비교 자동화 Fixture
  base_config: configs/local_benchmark_base.yaml
  evaluation_split: validation
  isolated_process: true
  continue_on_error: false
  models:
    - id: tiny
      backend: fixture
      repo_id: fixture/whisper-tiny
      revision: fixture
      device: cpu
      compute_type: int8
    - id: base
      backend: fixture
      repo_id: fixture/whisper-base
      revision: fixture
      device: cpu
      compute_type: int8
